.. This file should be included in Sphinx documentation and should contains
   simple information on how the project should be installed or tested. Use the
   `.. include:: path/to/README.rst` statement to include the file.

To launch tests of the project, use the following command:

.. code-block:: bash

  $ make check
